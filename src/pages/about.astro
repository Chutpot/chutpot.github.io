---
import Base from '../layouts/Base.astro';
import { getEntry } from 'astro:content';

const about = await getEntry('pages', 'about');
const { Content } = await about.render();
---

<Base title={`${about.data.title} — Chutpot Studio`} description={about.data.description}>
  <p class="text-sm font-semibold uppercase tracking-[0.2em] text-[color:var(--color-muted)]">About</p>
  <article class="prose max-w-3xl">
    <Content />
  </article>
</Base>
